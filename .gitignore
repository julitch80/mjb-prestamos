node_modules/
dist/
.claude/
.env.local
.env.*.local
.env

# Firebase
serviceAccountKey.json
docentes.csv
functions/node_modules
functions/lib
functions-asistencia/node_modules
functions-asistencia/lib
.firebase/
firebase-debug.log*
firestore-debug.log*

# Archivos temporales del parseo inicial
_horario_manana.html
_horario_manana_raw.txt
_horario_tarde_Hoja1.csv

# Agendas semanales en PDF: documentos institucionales que Julian deja aqui
# para que Claude las transcriba. NO se versionan — el repositorio es publico y
# traen nombres y actividades internas. Lo que se publica es la transcripcion en
# src/data/agendaSemanal.ts, no el archivo original.
Agenda/
